return {
  -- {
  --   "stevearc/conform.nvim",
  --   opts = function(_, opts)
  --     opts.formatters = opts.formatters or {}
  --     table.insert(opts.formatters.prettier, {
  --       prepend_args = { "--config", "/home/fireond/.config/." },
  --     })
  --     return opts
  --   end,
  -- },
}
